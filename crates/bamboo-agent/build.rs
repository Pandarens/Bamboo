fn main() {
    // Один входной файл импортирует оба окна: и виджет, и главное окно
    // попадают в одну генерацию.
    slint_build::compile("ui/app.slint").expect("не удалось собрать интерфейс");
}
