# Упаковка

Как Bamboo попадает к людям помимо страницы выпусков.

## scoop

`scoop/bamboo.json` — готовый манифест. Поставить прямо из репозитория:

```
scoop install https://raw.githubusercontent.com/Pandarens/Bamboo/main/packaging/scoop/bamboo.json
```

Чтобы Bamboo появился в общем каталоге, манифест надо подать в `scoop-extras`
отдельным запросом на слияние. Пока этого не сделано — ставится по ссылке
выше, и это работает так же.

`checkver` и `autoupdate` настроены на выпуски GitHub и на файл `sha256.txt`,
который кладёт рядом с программой рабочий процесс выпуска.

## winget

`winget/` — три файла манифеста схемы 1.12, готовые к подаче.

**Подпись для этого не нужна.** Прежняя редакция этого файла утверждала
обратное — что winget требует подписанного установщика, — и это было
неверно. В каталоге полно неподписанных portable-пакетов: `fzf`, `fd`
и прочие лежат там ровно в таком же виде, в каком собран Bamboo.
Проверено по самому каталогу, а не по памяти.

**Первая подача — только руками**: действие в рабочем процессе умеет
лишь обновлять пакет, который в каталоге уже есть («At least one version
of your package should already be present» — из его же документации;
пакета Pandarens.Bamboo в `microsoft/winget-pkgs` пока нет, проверено).
Подать первый раз проще всего инструментом `wingetcreate` от Microsoft,
скормив ему готовые манифесты из `winget/`.

После первой подачи обновления идут сами: шаг `winget` в рабочем процессе
выпуска открывает запрос на слияние с новой версией. Он срабатывает
**только на окончательных выпусках** — каталог winget не место для
предвыпусков, оттуда ставят не глядя.

Чтобы он заработал, нужен один секрет в настройках репозитория:
`WINGET_TOKEN` — личный токен с правом `public_repo`. Токен самого
репозитория не годится: запрос открывается в чужой репозиторий.

## Чего здесь нет и почему

**Подписи.** Её не будет — это решение, а не недоделка. Проект
открытый и бесплатный, а сертификат подписи кода стоит денег.
Самоподписанный сертификат от SmartScreen и Defender не спасает: они
доверяют не факту подписи, а репутации издателя. Ставить его ради галочки
значило бы сделать плацебо-функцию в смысле раздела 11.5 ТЗ — выглядит как
защита, ею не является.

Что есть вместо: подтверждение происхождения GitHub (кем, из какого коммита
и каким рабочим процессом собран файл) и SHA-256 в описании выпуска.

**Обещания воспроизводимой сборки.** `rust-toolchain.toml` закрепляет
`stable` — плавающий канал, а путей через `--remap-path-prefix` нет.
Провенанс отвечает на вопрос «кто и откуда собрал», а не «соберётся ли
байт в байт». Смешивать эти два обещания нечестно.
